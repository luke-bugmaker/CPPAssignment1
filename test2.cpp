#include <iostream>
#include <iomanip>

using namespace std;

void grid(int ** grid, int width, int height) {
    // upper border
    for (int x = -1; x <= width; x++) {
        cout << "# ";
    }
    cout << endl;

    // middle rows
    for (int y = )
}